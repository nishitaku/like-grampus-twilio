{
  "name": "like-grampus-twilio",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "twilio-run --env",
    "start:ngrok": "twilio-run --env --ngrok",
    "deploy": "twilio-run deploy"
  },
  "devDependencies": {
    "twilio-run": "^2.0.0"
  },
  "engines": {
    "node": "10.17.0"
  },
  "dependencies": {
    "@line/bot-sdk": "^6.8.4",
    "date-fns": "^2.9.0",
    "ibm-cos-sdk": "^1.6.0",
    "request": "^2.88.2",
    "request-promise": "^4.2.5"
  }
}
